// Blank service worker to resolve 404 errors and stop performance-blocking compilation cycles.
self.addEventListener('install', () => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim());
});
